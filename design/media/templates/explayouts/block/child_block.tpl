{* Render a child block inside a column without the outer ngl-block container *}
{include uri=concat('design:explayouts/block/', $block.definition_identifier, '.tpl') block=$block module_result=$module_result}
