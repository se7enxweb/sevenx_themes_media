{* tpl_block block_name = article_body.
   Renders the full-view body region (intro, body, tags, social share) as a
   standalone layout block. *}
{include uri='design:content/parts/full_body.tpl' module_result=$module_result}
